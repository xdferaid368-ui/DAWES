PRÁCTICA 1 — Configuración del proyecto y primera API
Objetivo
Crear un proyecto Django con Django REST Framework y exponer los modelos User y Group mediante una API.

Paso 1 — Crear entorno y proyecto
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install django djangorestframework
django-admin startproject config .
python manage.py startapp api
Paso 2 — Configurar settings.py
En INSTALLED_APPS añadir:

'rest_framework',
'api',
Aplicar migraciones y crear superusuario:

python manage.py migrate
python manage.py createsuperuser
Paso 3 — Crear serializers
En api/serializers.py:

from django.contrib.auth.models import User, Group
from rest_framework import serializers

class UserSerializer(serializers.HyperlinkedModelSerializer):
    class Meta:
        model = User
        fields = ['url', 'username', 'email', 'groups']

class GroupSerializer(serializers.HyperlinkedModelSerializer):
    class Meta:
        model = Group
        fields = ['url', 'name']
Paso 4 — Crear ViewSets
En api/views.py:

from django.contrib.auth.models import User, Group
from rest_framework import viewsets
from .serializers import UserSerializer, GroupSerializer

class UserViewSet(viewsets.ModelViewSet):
    queryset = User.objects.all()
    serializer_class = UserSerializer

class GroupViewSet(viewsets.ModelViewSet):
    queryset = Group.objects.all()
    serializer_class = GroupSerializer
Paso 5 — Registrar rutas con router
En config/urls.py:

from django.contrib import admin
from django.urls import path, include
from rest_framework import routers
from api.views import UserViewSet, GroupViewSet

router = routers.DefaultRouter()
router.register(r'users', UserViewSet)
router.register(r'groups', GroupViewSet)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/', include(router.urls)),
    path('api-auth/', include('rest_framework.urls')),
]
Paso 6 — Probar la API
Ejecutar servidor:

python manage.py runserver
Ir a:

http://127.0.0.1:8000/api/
Comprobar que:

Se ven los endpoints
Se puede navegar con la Browsable API
Requiere login para modificar

